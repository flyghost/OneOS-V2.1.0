#include "include.h"
#include "arbitrate.h"

OSStatus wifi_request_rf(uint8_t flag, uint8_t event, uint32_t use_time, uint32_t *wait_time)
{
    OSStatus ret;
    
    ret = get_event_priority(event, );
}